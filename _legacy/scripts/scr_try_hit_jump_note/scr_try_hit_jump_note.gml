function scr_try_hit_jump_note() {
    // Use the same hit logic you already use for lane notes.
    // Replace scr_try_hit_note() with YOUR existing function name.
    return scr_try_hit()(global.LANE_JUMP);
}
